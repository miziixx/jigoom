<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 1×1 위젯 빠른 추가 팝업 테마 — 홈 위에 카드만 뜬다.
         딤·검은 배경 없이 완전 투명(뒤 홈 화면이 그대로 보인다).
         AndroidManifest 의 QuickAddActivity 에서 android:theme 로 지정. -->
    <style name="Theme.Jigeum.QuickAdd" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>
</resources>
