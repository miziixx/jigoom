name: Build 지금 APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - claude/goal-app-mvp-sj7lun
    paths:
      - 'jigeum/**'
      - '.github/workflows/jigeum-apk.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: jigeum

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Generate Android platform scaffold
        run: |
          flutter create --org com.ziia --project-name jigeum --platforms android .
          # flutter create regenerates a default boilerplate test that references a
          # non-existent `MyApp` widget; drop it so `flutter test` runs only the real suite.
          rm -f test/widget_test.dart

      - name: Strip uncommitted font refs from pubspec
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path("pubspec.yaml")
          lines = p.read_text(encoding="utf-8").splitlines(keepends=True)
          out, skip = [], False
          for ln in lines:
              if ln.startswith("  fonts:"):
                  skip = True
                  continue
              if skip and ln.strip() and not ln.startswith(("    ", "  fonts")):
                  skip = False
              if not skip:
                  out.append(ln)
          p.write_text("".join(out), encoding="utf-8")
          print("stripped fonts section")
          PY

      - name: Install dependencies
        run: flutter pub get

      - name: Patch Android (widget receiver, permissions, label, minSdk)
        run: python3 tool/ci_patch_android.py

      - name: Drift code generation
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Analyze
        run: flutter analyze lib/ --no-fatal-infos --no-fatal-warnings

      - name: Run core tests
        run: flutter test

      - name: Build release APK (split per ABI)
        run: flutter build apk --release --split-per-abi

      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: jigeum-release-apk
          path: jigeum/build/app/outputs/flutter-apk/*.apk
          retention-days: 14
