name: Build 지금 APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - claude/goal-app-mvp-sj7lun
      - claude/jigeum-google-calendar-sync-lqeo5b
      - claude/voice-routing-spec-impl-ugttwn
      - claude/check-branch-status-9ve8j2
      - claude/notion-realtime-auto-share-d67ut5
      - claude/build-70-71-merge-d92npg
      - claude/jigeum-v2-rebuild-ua6x7r
    paths:
      - 'jigeum/**'
      - '.github/workflows/jigeum-apk.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: jigeum

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Generate Android platform scaffold
        run: |
          flutter create --org com.ziia --project-name jigeum --platforms android .
          # flutter create 가 넣는 기본 위젯 테스트(MyApp 참조)는 제거 — 우리 테스트만 사용.
          rm -f test/widget_test.dart

      - name: Strip uncommitted font refs from pubspec
        run: |
          python3 - <<'PY'
          from pathlib import Path
          # 번들 폰트 파일이 실제로 있으면 fonts: 선언을 유지한다.
          # (Pretendard 등 커밋된 폰트) 없을 때만 기본 폰트로 빌드하도록 스트립.
          if list(Path("assets/fonts").glob("*.otf")) or list(Path("assets/fonts").glob("*.ttf")):
              print("bundled font files present — keeping fonts: section")
              raise SystemExit(0)
          p = Path("pubspec.yaml")
          lines = p.read_text(encoding="utf-8").splitlines(keepends=True)
          out, skip = [], False
          for ln in lines:
              if ln.startswith("  fonts:"):
                  skip = True
                  continue
              if skip and ln.strip() and not ln.startswith(("    ", "  fonts")):
                  skip = False
              if not skip:
                  out.append(ln)
          p.write_text("".join(out), encoding="utf-8")
          print("stripped fonts section")
          PY

      - name: Install dependencies
        run: flutter pub get

      - name: Patch Android (widget receiver, permissions, label, minSdk)
        run: python3 tool/ci_patch_android.py

      - name: Drift code generation
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Analyze
        run: flutter analyze lib/ --no-fatal-infos --no-fatal-warnings

      - name: Run core tests
        run: flutter test

      # arm64 단일 APK 하나만 빌드 — 요즘 폰 전부 호환, 파일 1개(~10MB),
      # split-per-abi zip(28.7MB) 이 걸리던 첨부 용량제한도 회피.
      - name: Build release APK (arm64 only)
        run: flutter build apk --release --target-platform android-arm64

      # Actions 아티팩트는 임시(14일)·보조용 — 스토리지 쿼터가 차도 아래 릴리즈
      # 게시(영구 자산)는 막지 않도록 continue-on-error 로 둔다.
      - name: Upload APK
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: jigeum-arm64-apk
          path: jigeum/build/app/outputs/flutter-apk/app-release.apk
          retention-days: 14

      # main 에 올라오면 롤링 최신 릴리즈로, 연동 브랜치면 프리뷰 릴리즈로 게시
      # → 폰에서 바로 설치 가능한 APK 링크가 생긴다.
      - name: Publish APK to Release
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/claude/jigeum-google-calendar-sync-lqeo5b' || github.ref == 'refs/heads/claude/voice-routing-spec-impl-ugttwn' || github.ref == 'refs/heads/claude/check-branch-status-9ve8j2' || github.ref == 'refs/heads/claude/notion-realtime-auto-share-d67ut5' || github.ref == 'refs/heads/claude/build-70-71-merge-d92npg' || github.ref == 'refs/heads/claude/jigeum-v2-rebuild-ua6x7r'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: >-
            ${{ github.ref == 'refs/heads/main' && 'jigeum-latest'
             || github.ref == 'refs/heads/claude/voice-routing-spec-impl-ugttwn' && 'jigeum-voice-preview'
             || github.ref == 'refs/heads/claude/check-branch-status-9ve8j2' && 'jigeum-widget-studio-preview'
             || github.ref == 'refs/heads/claude/notion-realtime-auto-share-d67ut5' && 'jigeum-notion-preview'
             || github.ref == 'refs/heads/claude/build-70-71-merge-d92npg' && 'jigeum-70-71-merge-preview'
             || github.ref == 'refs/heads/claude/jigeum-v2-rebuild-ua6x7r' && 'gomgom-v2-preview'
             || 'jigeum-gcal-preview' }}
          name: >-
            ${{ github.ref == 'refs/heads/main' && '지금 (Editorial) — 최신 APK'
             || github.ref == 'refs/heads/claude/voice-routing-spec-impl-ugttwn' && '지금 — 음성 라우팅 프리뷰 APK'
             || github.ref == 'refs/heads/claude/check-branch-status-9ve8j2' && '지금 — 위젯 스튜디오 프리뷰 APK'
             || github.ref == 'refs/heads/claude/notion-realtime-auto-share-d67ut5' && '지금 — 노션 자동공유 프리뷰 APK'
             || github.ref == 'refs/heads/claude/build-70-71-merge-d92npg' && '지금 — 70·71 병합 프리뷰 APK'
             || github.ref == 'refs/heads/claude/jigeum-v2-rebuild-ua6x7r' && '곰곰 (지금 v2) — 재현 프리뷰 APK'
             || '지금 — 구글 캘린더 연동 프리뷰 APK' }}
          body: |
            `지금` 앱 빌드 (arm64 release).
            커밋 ${{ github.sha }} 기준 자동 게시.

            설치: 아래 app-release.apk 를 폰에서 받아 실행 → "출처를 알 수 없는 앱" 허용 후 설치.
          files: jigeum/build/app/outputs/flutter-apk/app-release.apk
          make_latest: ${{ github.ref == 'refs/heads/main' }}
          prerelease: ${{ github.ref != 'refs/heads/main' }}
